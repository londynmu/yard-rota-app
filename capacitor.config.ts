import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
	appId: 'com.yard.rota',
	appName: 'Yard Rota',
	webDir: 'dist',
	server: {
		androidScheme: 'https',
	},
};

export default config;


