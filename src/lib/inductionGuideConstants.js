/** Supabase Storage bucket for induction guide inline images (public read). */
export const INDUCTION_GUIDE_IMAGE_BUCKET = 'induction-guide-images';

/** Max upload size (matches migration file_size_limit). */
export const INDUCTION_GUIDE_MAX_IMAGE_BYTES = 5 * 1024 * 1024;
